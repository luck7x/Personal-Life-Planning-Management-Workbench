# 明心台 2.0 ToDo

> 本文件从 2026-05-28 起作为重构路线入口。旧版 `明心台.html` 视为已验证 MVP 和归档参考，不再继续作为主要扩展对象。

## 状态标记

- `[x]` 已完成
- `[~]` 进行中
- `[ ]` 待开始
- `[!]` 阻塞 / 需确认

## 当前路线图

1. [x] 产出手机优先重构蓝图
   - 输出：`docs/superpowers/specs/2026-05-28-mingxintai-2-mobile-first-redesign.md`
   - 核心结论：先做手机优先 PWA，稳定后再考虑 Capacitor 打包 Android。

2. [x] 确认 2.0 MVP 范围
   - 首页：备忘、今日执行、未来待办、想法池、已完成折叠。
   - 项目：独立入口，不抢首页第一视线。
   - 复盘：自动摘要 + Markdown 自由输入。
   - 养息：轻记录，不做医疗化系统。
   - 设置：账号、通知、AI、数据。
   - 样例：`docs/prototypes/mingxintai-2-mobile-sample.html`
   - 截图：`docs/prototypes/mingxintai-2-mobile-sample-390.png`

3. [x] 产出手机优先静态样例页
   - 输出：`docs/prototypes/mingxintai-2-mobile-sample.html`
   - 验证：已用 Playwright 以 390x844 手机宽度截图。
   - 说明：这是方向样例，不接后端，不替代正式工程。
   - 配色：已加入 5 套可点选主题；2026-05-28 已替换为第二批更高明度方案，并生成对比截图；当前默认暂定为 `纸墨金章 / ink`。
   - 视觉：`正在专注` 卡片已改为浅色重点卡，不再使用深色整块。
   - 复盘：自由反思已补实时 Markdown 预览，并支持 `?view=review` 直接打开复盘页便于验收。

4. [x] 新建 2.0 前端工程
   - 推荐：React + Vite + Tailwind + PWA。
   - 先做静态交互原型，不急着接后端。
   - 输出：`frontend/`
   - 已迁移当前满意样例：底部导航、今天 / 项目 / 复盘 / 养息 / 设置、快速添加弹窗、复盘 Markdown 实时预览、纸墨金章默认主题、浅色正在专注卡片。
   - 验证：`npm run lint`、`npm run build` 通过；已生成 390x844 首页和复盘页截图。

5. [x] 手机端首页原型
   - 验收：早上打开 10 秒内知道今天先做什么。
   - 今日执行支持拖拽排序。
   - 快速添加不超过 5 秒完成。
   - 已实现：快速添加可写入对应分区；今日执行支持拖拽排序，并提供上下移动按钮兼容手机操作。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；测试 agent 已用 Playwright 本地 Chrome 独立验收。

6. [x] 专注 / 休息主流程
   - 任务和子任务都能开始专注。
   - 休息能暂停当前专注，结束后询问是否恢复。
   - 超长专注需要提醒和真实结束时间校正。
   - 已实现：任务 / 子任务启动专注、预计时长与提前提醒配置、一键休息暂停、休息全屏倒计时、结束休息后询问恢复、结束专注时校正真实结束时间并写入今日时间线。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；测试 agent 已用 Playwright 本地 Chrome 完成模拟人工验收。
   - 截图：`docs/prototypes/mingxintai-2-todo6-manual-390.png`

7. [x] 复盘与时间线
   - 自动汇总当天时间块、完成任务、未完成任务、未完成备忘。
   - 用户自由 Markdown 反思。
   - 已实现：今日时间线改为真实专注 / 休息记录驱动，复盘页自动汇总已完成、未完成任务、未完成备忘，并保留 Markdown 实时预览。
   - 验证：`npm run test:e2e` 覆盖真实用户快速添加备忘、添加今日执行、专注、休息、恢复、结束专注、进入复盘、输入 Markdown 反思。
   - 截图：`docs/prototypes/mingxintai-2-todo7-review-390.png`

7.1 [x] 补齐专注实时记录、任务提醒输入和项目新增
   - 专注开始后，今日时间线必须实时出现正在增长的专注块。
   - 一键休息暂停专注时，时间线必须保留休息前的专注段，同时展示休息块。
   - 快速添加任务需要更顺手的时间输入和提醒配置，先做前端弹窗/表单层，WxPusher 推送留给后端接入。
   - 项目页必须能新增项目，避免“项目只是展示不能使用”。
   - 已实现：专注 live block、休息前专注段固化、休息 live block、快速添加时间选择与通知偏好、项目页新增项目入口。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；已用 Playwright 本地 Chrome 模拟开始专注、一键休息并截图。
   - 截图：`docs/prototypes/mingxintai-2-live-focus-rest-390.png`

8. [x] 养息轻记录
   - 默认记录：睡眠、起床、运动、饮食、饮水、身高、体重。
   - 自定义习惯支持单项 / 多项。
   - 已实现：单项记录只保留最新，多项记录追加展示；时间统一为开始 / 结束，相同时间只显示一个时间，有差值则显示持续时长。
   - 已实现：支持自定义习惯，创建时选择单项 / 多项，可填写单位。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；测试 agent 已用 Playwright 本地 Chrome 验收默认记录、多条饮水、自定义单项 / 多项习惯和移动端无横向溢出。
   - 截图：`docs/prototypes/mingxintai-2-health-records-390.png`；测试 agent 附加截图在 `frontend/output/playwright/`。

8.1 [x] 对照旧版补齐任务添加
   - 旧版字段对照：任务名称、所属项目、紧急程度、状态、所在分区、计划日期、到期日期、开始 / 结束时间、预计分钟、通知覆盖、子任务。
   - 已实现：快速添加支持所属项目、所在分区、任务状态、紧急程度、计划日期、到期日期、开始 / 结束时间、预计分钟、通知提前时间和多行子任务。
   - 验收：创建任务时可选不同分区，不再只能进入今日；可关联项目；可添加子任务；任务卡能展示关键字段且不挤压移动端。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；已用 Playwright 390x844 模拟用户创建未来待办、关联导师项目、填写日期时间和子任务，无横向溢出。
   - 截图：`docs/prototypes/mingxintai-2-task-add-complete-390.png`

8.2 [x] 补回任务详情与项目任务管理
   - 背景：2.0 当前只能在创建任务时填写子任务，已有任务不能继续追加；项目页只能展示项目，不能像旧版那样看到项目下的任务。
   - 已实现：任务卡增加“管理”入口，打开任务详情抽屉，可追加子任务；项目页增加项目任务列表和全部代办列表。
   - 验收：已有任务可追加子任务并立即显示；新增子任务可单独专注；项目页能看到归属任务；全部代办能覆盖备忘、今日执行、未来待办、想法池等分区。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；测试 agent 已用 Playwright 定向复核，390x844 移动端无横向溢出。
   - 截图：`docs/prototypes/mingxintai-2-task-detail-subtask-390.png`、`docs/prototypes/mingxintai-2-project-task-board-390.png`

8.3 [x] 优化全部代办分区与任务状态可读性
   - 背景：全部代办需要按分区展示；任务列表需要显示时间 / 到期 / 原摘要；子任务完成需要明显标注；`排入`、`看看` 属于无效占位按钮。
   - 已实现：全部代办按备忘、今日执行、未来待办、想法池等分区展示；任务摘要合并分区、状态、计划 / 到期 / 时间、原 meta 和子任务进度；完成子任务使用绿色完成态和删除线；项目任务里的完成子任务名前加 `✓`；非今日任务不再显示无效动作按钮，只保留“管理”。
   - 验收：项目页全部代办可按分区扫描；未来待办能看到 `剩 2 天` 等时间信息；完成子任务视觉可区分；页面上不再出现无行为的 `排入`、`看看` 按钮。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；测试 agent 用 Playwright 390x844 复核分区、元信息、完成样式、无效按钮移除和横向溢出。

8.4 [x] 任务管理支持编辑任务属性和子任务编辑删除
   - 背景：已创建任务的“管理”抽屉只能查看项目 / 分区 / 状态，不能继续调整；子任务只能新增 / 勾选，不能修改或删除。
   - 验收：任务管理内可修改所属项目、所在分区、任务状态；保存后任务移动到对应分区 / 项目；子任务可改标题、开始日期、到期日期，也可删除。
   - 已实现：任务管理抽屉增加所属项目、所在分区、任务状态编辑；保存后重建任务摘要并同步项目页 / 分区；子任务支持编辑标题、开始日期、到期日期和删除。
   - 验证：先新增失败用例确认缺口，再实现；`npm run lint`、`npm run build`、`npm run test:e2e` 均通过，新增 Playwright 用例覆盖移动端抽屉无横向溢出和按钮尺寸。

8.5 [x] 补任务完成勾选、周视图和真实一周路径测试
   - 背景：任务卡缺少一键打勾完成入口；底部导航缺少周视图；需要用真实用户一天到一周的操作路径找问题。
   - 返工原因：上一版把周视图误做成 7 天清单，用户实际要旧版“流光周历”式周时间轴：左侧小时刻度、横向 7 天列、时间块定位展示。
   - 验收：任务卡可直接勾选完成并移动到已完成；子任务在任务卡上可直接勾选，全部完成后父任务自动完成；周视图可展示 6:00-24:00 时间轴、7 天列、时间块；支持手动补录时间块并编辑；Playwright 覆盖创建、完成、查看周视图、补录/编辑、项目、复盘、养息等连续路径。
   - 追加反馈：项目页“全部代办”中不同分区必须有明显颜色区分，不能只靠标题文字识别。
   - 进展：已为全部代办的备忘、今日执行、未来待办、想法池等分区补充分区级语义色、色条、标题色和 Playwright 断言；截图 `frontend/docs/prototypes/mingxintai-2-task-board-colors-390.png`。
   - 已实现：任务卡可直接勾选完成并进入已完成；子任务在首页任务卡可直接勾选，子任务全完成后父任务自动进入已完成。
   - 已实现：周览改为真正“流光周历”时间轴，包含 6:00-24:00 左侧刻度、7 天列、任务 / 完成记录 / 时间线记录定位展示。
   - 已实现：周览默认横向定位到今天附近，左侧时间栏 sticky 固定可见；点击任意时间块即可打开编辑弹窗；补录时间块支持新增和编辑。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；Playwright 额外验证今天列居中、时间栏 sticky、时间块 role=button 可点击编辑。
   - 截图：`frontend/docs/prototypes/mingxintai-2-week-timeline-390.png`、`frontend/docs/prototypes/mingxintai-2-week-block-edit-390.png`。

9. [~] 接入现有后端
   - 后端作为权威数据源。
   - 前端不再把 localStorage 当主存储。
   - 同步、通知、AI 都由后端支撑。
   - WxPusher：任务提醒偏好已在前端记录，后续由后端定时任务读取并推送。
   - Android：系统级弹窗 / 通知放到 PWA 稳定后与 Capacitor 一起评估。
   - [x] 9.1 设置页补 WxPusher 配置：SPT 默认空白，支持后端 API 地址、访问密钥、任务默认提前提醒、专注提醒和兜底分钟。
   - [x] 9.2 设置页支持保存配置和发送测试通知：调用后端 `/api/notifications/test`，不硬编码任何用户 SPT；配置保存到当前设备浏览器，刷新后保留。
   - [x] 9.3 迁移旧版专注超时策略：预计结束前提醒、预计到点提醒、超过兜底分钟后自动暂停，并弹出真实结束时间校正。
   - 验证：`npm run lint`、`npm run build`、`npm run test:e2e` 均通过；后端测试已用项目 `.venv` 通过；设置页截图 `frontend/docs/prototypes/mingxintai-2-wxpusher-settings-390.png`。
   - [x] 9.4 前端状态正式接后端状态 API：任务、子任务、专注、休息、复盘、养息和通知配置不再只存前端内存；后端地址和访问密钥仍只保存在当前设备。
   - [x] 9.5 后台提醒灰度：VPS 定时扫描可读取 2.0 写入后端的通知配置，浏览器关闭时也能基于后端状态推送。
   - 验证：新增 Playwright 用例覆盖后端状态恢复、修改后自动保存、复盘/养息恢复；新增后端用例覆盖 reminder scan 读取前端通知配置；`npm run lint`、`npm run build`、`npm run test:e2e`、`PYTHONPATH=backend python -m unittest discover -s backend/tests` 均通过。

10. [~] VPS 灰度部署
   - 保留旧版可访问。
   - 新版独立路径灰度验证。
   - 本地已完成生产构建；待推送 GitHub 后在 VPS 拉取并切换部署目录。

11. [x] 评估 Capacitor Android 打包
    - 前提：PWA 主流程稳定。
    - 准备：Android Studio、Android SDK、Node.js、App 图标、包名、签名密钥。
    - 结论：当前先作为 PWA 上线更稳；等后端权威数据、通知后台和移动端主流程稳定后，再进入 Capacitor 打包。Android 15 适配主要准备包名、图标、签名密钥、通知权限和 WebView 兼容测试，不应在当前阶段抢主线。

## 硬约束

- 不为了系统完整而加功能。
- 不在旧单文件 HTML 上继续堆主要功能。
- 首页永远服务“今天怎么过”。
- 项目、统计、AI、设置都不能抢今日执行的主视线。
- 每轮开发前先读本文件和重构蓝图。
